---
import BaseLayout from '../../layouts/BaseLayout.astro';
import HomepageDevotional from '../../components/HomepageDevotional.astro';
import { useTranslations } from '../../i18n/utils';

const pericopeModules = import.meta.glob('../../content/luke-1-pericopes-*.json', { eager: true });

export function getStaticPaths() {
  return [
    { params: { lang: 'da' } },
    { params: { lang: 'de' } },
    { params: { lang: 'en' } },
    { params: { lang: 'es' } },
    { params: { lang: 'fr' } },
    { params: { lang: 'it' } },
    { params: { lang: 'pl' } },
    { params: { lang: 'pt' } },
    { params: { lang: 'sv' } },
    { params: { lang: 'tr' } },
    { params: { lang: 'uk' } },
    { params: { lang: 'nl' } },
    { params: { lang: 'ro' } },
    { params: { lang: 'cs' } },
    { params: { lang: 'el' } },
    { params: { lang: 'hu' } },
    { params: { lang: 'bg' } },
    { params: { lang: 'hr' } },
    { params: { lang: 'fi' } },
    { params: { lang: 'sk' } },
    { params: { lang: 'lt' } },
    { params: { lang: 'sl' } },
    { params: { lang: 'lv' } },
    { params: { lang: 'et' } },
    { params: { lang: 'ga' } },
    { params: { lang: 'mt' } },
    { params: { lang: 'nb' } },
    { params: { lang: 'ru' } },
    { params: { lang: 'ar' } },
    { params: { lang: 'ca' } },
    { params: { lang: 'sq' } },
  ];
}

const { lang } = Astro.params as { lang: string };
const t = useTranslations(lang);

const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith('/') ? rawBase : rawBase + '/';

// Pick whatever morning + evening devotional this language has wired on Luke 1.
// Different languages have their single reflexionen on different pericopes
// (verkuendigung / magnificat / annunciation / benedictus / ...), so walk the
// pericopes array and grab the first entry of each kind.
type DevotionalEntry = { title: string; mp3: string; duration?: string };
const pericopeData = (pericopeModules[`../../content/luke-1-pericopes-${lang}.json`] as any)?.default;
let morningDevotional: DevotionalEntry | null = null;
let eveningDevotional: DevotionalEntry | null = null;
if (pericopeData?.pericopes) {
  for (const p of pericopeData.pericopes) {
    const m = p?.media || {};
    if (!morningDevotional && m.devotional?.mp3) morningDevotional = m.devotional;
    if (!eveningDevotional && m.reflexionen?.mp3) eveningDevotional = m.reflexionen;
    if (morningDevotional && eveningDevotional) break;
  }
}

// Language-specific book names for Luke
const lukeNames: Record<string, string> = {
  da: 'Lukas',
  de: 'Lukas',
  en: 'Luke',
  es: 'Lucas',
  fr: 'Luc',
  it: 'Luca',
  pl: 'Łukasz',
  pt: 'Lucas',
  sv: 'Lukas',
  tr: 'Luka',
  uk: 'Луки',
  nl: 'Lucas',
  ro: 'Luca',
  cs: 'Lukáš',
  el: 'Λουκάς',
  hu: 'Lukács',
  bg: 'Лука',
  hr: 'Luka',
  fi: 'Luukas',
  sk: 'Lukáš',
  lt: 'Luko',
  sl: 'Luka',
  lv: 'Lūka',
  et: 'Luuka',
  ga: 'Lúcás',
  mt: 'Luqa',
  nb: 'Lukas',
  ru: 'Луки',
  ar: 'لوقا',
  ca: 'Lluc',
  sq: 'Luka',
};
---

<BaseLayout title={t('nav.home')} lang={lang} heroPage={true}>
  <!-- Video Hero -->
  <section class="video-hero">
    <!-- Background video -->
    <video
      class="video-bg"
      id="hero-video"
      poster={`${base}images/homepage_hero_sunrise_low.png`}
      autoplay
      loop
      muted
      playsinline
      preload="metadata"
      aria-hidden="true"
    ></video>
    <!-- Color overlay -->
    <div class="video-overlay" aria-hidden="true"></div>
    <!-- Grain overlay -->
    <div class="video-grain" aria-hidden="true" style={`background-image: url(${base}images/grain.gif)`}></div>
    <!-- Content -->
    <div class="video-hero-content">
      <h1 class="hero-wordmark tracking-tight logo">aperto</h1>
      <p class="hero-tagline">
        {t('home.tagline')}
      </p>
      <div class="flex justify-center items-center">
        {(['de', 'en', 'pl'].includes(lang)) ? (
          <a href="#sample-chapters" class="hero-btn">
            {lang === 'de' ? 'Probekapitel ansehen' : lang === 'pl' ? 'Zobacz rozdziały próbne' : 'Browse sample chapters'}
          </a>
        ) : (
          <a href={`/${lang}/luke/`} class="hero-btn">
            {t('home.readLuke')}
          </a>
        )}
      </div>
    </div>
  </section>

  <script define:vars={{ base }}>
    (function () {
      var v = document.getElementById('hero-video');
      if (!v) return;
      var mq = window.matchMedia('(prefers-color-scheme: dark)');
      function apply() {
        var dark = mq.matches;
        var next = base + 'videos/' + (dark ? 'hero-dark.mp4' : 'hero-light.mp4');
        if (v.getAttribute('src') === next) return;
        v.setAttribute('poster', base + 'images/' + (dark ? 'homepage_hero_blue_hour_low.png' : 'homepage_hero_sunrise_low.png'));
        v.setAttribute('src', next);
        v.load();
        var p = v.play();
        if (p && p.catch) p.catch(function () {});
      }
      apply();
      if (mq.addEventListener) mq.addEventListener('change', apply);
      else if (mq.addListener) mq.addListener(apply);
    })();
  </script>

  <style>
    /* Prevent blurred video from bleeding horizontally */
    :global(body) {
      overflow-x: hidden;
    }

    /* Video Hero */
    .video-hero {
      position: relative;
      width: 100%;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      clip-path: inset(0);
    }

    /* Bottom fade only — left/right stay clean */
    .video-hero::after {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(to bottom, transparent 50%, #FFFBF5 100%);
      z-index: 4;
      pointer-events: none;
    }

    /* Bulletproof fullscreen video: 130% size, centered via translate.
       The 15% overhang on every side hides the blur's transparent fringe
       (blur radius ≈ 3× the 30px sigma) outside the parent's overflow clip.
       Avoids over-constrained left/right/width absolute-positioning quirks. */
    .video-bg {
      position: absolute;
      top: 50%;
      left: 50%;
      width: 130%;
      height: 130%;
      transform: translate(-50%, -50%);
      object-fit: cover;
      object-position: center center;
      filter: blur(30px);
      -webkit-filter: blur(30px);
      z-index: 0;
    }

    .video-overlay {
      position: absolute;
      inset: 0;
      background: rgba(38, 22, 12, 0.24);
      z-index: 1;
    }

    .video-grain {
      position: absolute;
      inset: 0;
      opacity: 0.09;
      background-repeat: repeat;
      background-size: 128px auto;
      z-index: 2;
    }

    .video-hero-content {
      position: relative;
      z-index: 3;
      text-align: center;
      padding: 2rem;
      max-width: 56rem;
      margin: 0 auto;
    }

    .hero-wordmark {
      font-size: clamp(5rem, 16vw, 14rem);
      line-height: 1;
      color: #FFFBF5;
      margin-bottom: 1.5rem;
      text-shadow: 0 2px 32px rgba(0,0,0,0.25);
    }

    .hero-tagline {
      font-size: clamp(1rem, 2.2vw, 1.4rem);
      line-height: 1.6;
      color: rgba(255, 251, 245, 0.82);
      max-width: 36rem;
      margin: 0 auto 2.5rem;
      font-weight: 300;
    }

    /* Lighter Switzer for body copy on the homepage.
       Switzer ships 150 (Regular) + 500 (Medium); 300 rounds to 150. */
    section p {
      font-weight: 300;
    }

    .hero-btn {
      display: inline-block;
      padding: 0.9rem 2.2rem;
      border-radius: 6px;
      font-weight: 500;
      background-color: rgba(255, 251, 245, 0.15);
      color: #FFFBF5;
      border: 1px solid rgba(255, 251, 245, 0.35);
      backdrop-filter: blur(8px);
      -webkit-backdrop-filter: blur(8px);
      transition: background-color 0.2s, border-color 0.2s;
      text-decoration: none;
    }

    .hero-btn:hover {
      background-color: rgba(255, 251, 245, 0.26);
      border-color: rgba(255, 251, 245, 0.55);
    }

    .video-hero::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 130%;
      height: 130%;
      transform: translate(-50%, -50%);
      background-image: url('/images/homepage_hero_sunrise_low.png');
      background-size: cover;
      background-position: center;
      filter: blur(30px);
      -webkit-filter: blur(30px);
      z-index: 0;
      opacity: 0;
      transition: opacity 0.3s;
    }

    @media (prefers-reduced-motion: reduce) {
      .video-bg { display: none; }
      .video-hero::before { opacity: 1; }
    }

    /* Dark mode: reduced-motion fallback still shows the blue-hour still */
    @media (prefers-color-scheme: dark) {
      .video-hero::before { background-image: url('/images/homepage_hero_blue_hour_low.png'); }
    }

    /* Get Involved section */
    .get-involved-section {
      background-color: var(--color-charcoal);
      color: var(--color-cream);
    }

    .get-involved-section h2 {
      color: var(--color-cream);
    }

    .get-involved-section p {
      color: rgba(255, 251, 245, 0.7);
    }

    .get-involved-btn {
      background-color: var(--color-cream);
      color: var(--color-charcoal);
    }

    @media (prefers-color-scheme: dark) {
      .get-involved-section {
        background-color: #111111;
        color: #f0ebe3;
      }

      .get-involved-section h2 {
        color: #f0ebe3;
      }

      .get-involved-section p {
        color: rgba(240, 235, 227, 0.65);
      }

      .get-involved-btn {
        background-color: #f0ebe3;
        color: #111111;
      }
    }
  </style>

  <HomepageDevotional lang={lang} morning={morningDevotional} evening={eveningDevotional} />

  <section class="py-24 px-8 md:px-16 lg:px-24" style="background-color: rgba(245, 230, 220, 0.3);">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-3xl md:text-4xl mb-12 text-center headline-mixed">{t('home.differentBible')}</h2>
      <div class="grid md:grid-cols-3 gap-12">
        <div class="text-center">
          <h3 class="text-xl mb-3">{t('home.accessible')}</h3>
          <p class="leading-relaxed" style="color: #6A6A6A;">
            {t('home.accessibleDesc')}
          </p>
        </div>
        <div class="text-center">
          <h3 class="text-xl mb-3">{t('home.multimedia')}</h3>
          <p class="leading-relaxed" style="color: #6A6A6A;">
            {t('home.multimediaDesc')}
          </p>
        </div>
        <div class="text-center">
          <h3 class="text-xl mb-3">{t('home.openSource')}</h3>
          <p class="leading-relaxed" style="color: #6A6A6A;">
            {t('home.openSourceDesc')}
          </p>
        </div>
      </div>
    </div>
  </section>

  {/* Trial-sampler grid: shows all 6 sample chapters in DE/EN/PL.
       Other languages keep the original single-CTA-to-Luke-1 layout. */}
  {(['de', 'en', 'pl'].includes(lang)) ? (
    <section id="sample-chapters" class="py-24 px-8 md:px-16 lg:px-24">
      <div class="max-w-6xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-3xl md:text-4xl mb-4 headline-mixed">{t('home.startReading')}</h2>
          <p class="text-lg leading-relaxed max-w-2xl mx-auto" style="color: #6A6A6A;">
            {t('home.startReadingDesc')}
          </p>
        </div>
        <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {([
            { href: 'luke/1',     label: lang === 'de' ? 'Lukas 1' : lang === 'pl' ? 'Łukasz 1' : 'Luke 1',
              sub: lang === 'de' ? 'Verkündigung & Geburt' : lang === 'pl' ? 'Zwiastowanie i narodziny' : 'Annunciation & Birth',
              img: '/images/luke/1/luke_01_annunciation.png' },
            { href: 'luke/15',    label: lang === 'de' ? 'Lukas 15' : lang === 'pl' ? 'Łukasz 15' : 'Luke 15',
              sub: lang === 'de' ? 'Die verlorenen Söhne' : lang === 'pl' ? 'Zagubieni synowie' : 'The Lost Sons',
              img: '/images/luke/15/luke_15_prodigal-son.png' },
            { href: 'psalms/23',  label: lang === 'pl' ? 'Psalm 23' : 'Psalm 23',
              sub: lang === 'de' ? 'Der Herr ist mein Hirte' : lang === 'pl' ? 'Pan jest moim pasterzem' : 'The Lord Is My Shepherd',
              img: '/images/psalms/23/psalms_023_shepherd.png' },
            { href: 'psalms/137', label: 'Psalm 137',
              sub: lang === 'de' ? 'An Babels Strömen' : lang === 'pl' ? 'Nad rzekami Babilonu' : 'By the Rivers of Babylon',
              img: '/images/psalms/137/psalms_137_rivers.png' },
            { href: 'psalms/139', label: 'Psalm 139',
              sub: lang === 'de' ? 'Du hast mich erforscht' : lang === 'pl' ? 'Przenikasz i znasz mnie' : 'You Have Searched Me',
              img: '/images/psalms/139/psalms_139_searched.png' },
            { href: 'acts/17',    label: lang === 'de' ? 'Apostelgeschichte 17' : lang === 'pl' ? 'Dzieje 17' : 'Acts 17',
              sub: lang === 'de' ? 'Rede auf dem Areopag' : lang === 'pl' ? 'Mowa na Areopagu' : 'Speech on the Areopagus',
              img: '/images/acts/17/acts_017_areopagus.png' },
          ]).map((c) => (
            <a
              href={`${base}${lang}/${c.href}`}
              class="trial-card group block rounded-lg overflow-hidden transition-all hover:shadow-lg"
              style="background-color: #FFFBF5; border: 1px solid rgba(139, 115, 85, 0.15);"
            >
              <div class="aspect-[4/3] overflow-hidden" style="background-color: #F5E6DC;">
                <img src={c.img} alt="" class="w-full h-full object-cover transition-transform group-hover:scale-105" loading="lazy" />
              </div>
              <div class="p-5">
                <h3 class="text-xl mb-1" style="color: #2A2A2A;">{c.label}</h3>
                <p class="text-sm" style="color: #6A6A6A;">{c.sub}</p>
              </div>
            </a>
          ))}
        </div>
      </div>
    </section>
  ) : (
    <section class="py-24 px-8 md:px-16 lg:px-24">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl md:text-4xl mb-8 headline-mixed">{t('home.startReading')}</h2>
        <p class="text-lg leading-relaxed mb-12" style="color: #6A6A6A;">
          {t('home.startReadingDesc')}
        </p>
        <a
          href={`${base}${lang}/luke/1`}
          class="inline-block px-8 py-4 rounded font-medium transition-all hover:opacity-90"
          style="background-color: #8B7355; color: #FFFBF5;"
        >
          {lukeNames[lang]} 1
        </a>
      </div>
    </section>
  )}

  <!-- Get Involved -->
  <section class="get-involved-section py-24 px-8 md:px-16 lg:px-24">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-3xl md:text-4xl mb-6">{t('home.getInvolved.title')}</h2>
      <p class="text-lg leading-relaxed mb-10">
        {t('home.getInvolved.description')}
      </p>
      <button
        class="get-involved-btn px-8 py-4 rounded font-medium transition-all hover:opacity-90 cursor-pointer"
      >
        {t('home.getInvolved.button')}
      </button>
    </div>
  </section>

  <script is:inline>
    document.querySelector('.get-involved-btn')?.addEventListener('click', function() {
      if (typeof Tally !== 'undefined') {
        Tally.openPopup('b58vj1', { width: 600 });
      } else {
        window.open('https://tally.so/r/b58vj1', '_blank');
      }
    });
  </script>
</BaseLayout>
