---
interface Props {
  text: string;
  tag?: 'h1' | 'h2' | 'h3' | 'h4';
  class?: string;
}

const { text, tag = 'h1', class: className = '' } = Astro.props;
const Tag = tag;
---

<Tag class:list={['headline-mixed', className]}>{text}</Tag>
